my_dict = {'a':1, 'b':2}
print(my_dict.get('c', 'default')) # usa o método .get() para pegar o valor associado à chave 'c' do dicionário.
                                   # neste caso, a chave 'c' não existe em my_dict, então o valor padrão é retornado como default

# >> default
