marking_period_scores = [96, 95, 99, 97]

avg_mp_scores = (sum (marking_period_scores)) // len(marking_period_scores) # sum(...) / len () = 96.75 , mas como a questao quer o módulo, fica-se apenas com a parte real, ou seja 96
print(avg_mp_scores)

# >> 96
