# Lambda Function: are small anonymous functions defined useing the lambda keyword.
# They are useful for simple operations in a single line

# 1. Lambda Function (a lambda function that adds two numbers together)
add = lambda a,b: a+b

result = add(3,4)
print(result)

# >> 7
